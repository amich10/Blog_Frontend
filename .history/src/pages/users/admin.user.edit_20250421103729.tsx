import notifcation from "../../utilities/helpers"

const UserEdit = () => {

    const getUsers = async() =>{
        try {
            
        } catch (exception) {
            notifcation("Sorry the requested users cannot be retrieved now.Please try again later.")
        }
    }

    return (
        <>
        
        </>
    )
}
export default UserEdit