import { createSlice } from "@reduxjs/toolkit";


interface AuthS

const initialState = {
    userdetails : null
}

const authSlice = createSlice({
    name:"auth",
    initialState,
    reducers:{

    }
})
