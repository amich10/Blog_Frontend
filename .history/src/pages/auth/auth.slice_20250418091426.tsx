import { createSlice } from "@reduxjs/toolkit";


interface 

const initialState = {
    userdetails : null
}

const authSlice = createSlice({
    name:"auth",
    initialState,
    reducers:{

    }
})
