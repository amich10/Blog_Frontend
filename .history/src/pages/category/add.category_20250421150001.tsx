import { useForm } from "react-hook-form";

const AddCategory = () =>{

    const {handleSubmit,formState:{}} = useForm()

    return (
        <>
        
        </>
    )
}

export default AddCategory;
