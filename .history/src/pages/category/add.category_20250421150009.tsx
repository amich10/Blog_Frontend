import { useForm } from "react-hook-form";

const AddCategory = () =>{

    const {handleSubmit,formState:{errors,isSubmitting},se} = useForm()

    return (
        <>
        
        </>
    )
}

export default AddCategory;
